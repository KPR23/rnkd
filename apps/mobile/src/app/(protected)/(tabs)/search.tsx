import { Text, View } from "react-native";

export default function SearchTab() {
	return (
		<View className="flex-1 items-center justify-center bg-background">
			<Text className="font-sans text-white">Search</Text>
		</View>
	);
}
